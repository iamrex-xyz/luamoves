# The Lovable template ships a peer-dependency conflict (@capacitor/haptics@8
# wants @capacitor/core>=8, but the project pins core@7.4.4). Resolve it the
# npm-v6 way so `npm ci` / `npm install` succeed deterministically here and in CI.
legacy-peer-deps=true
